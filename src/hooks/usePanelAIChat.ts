import { useThemedAIChat } from './useThemedAIChat';
import type { UsePanelAIChatOptions, UseThemedAIChatReturn } from '../types';

export function usePanelAIChat(options: UsePanelAIChatOptions): UseThemedAIChatReturn {
  // Placeholder - will integrate with panel context
  return useThemedAIChat(options);
}
